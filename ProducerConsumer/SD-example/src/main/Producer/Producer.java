package main.Producer;


import java.util.logging.Level;
import java.util.logging.Logger;

public class Producer {
    final private String PRODUCER_CLASS_NAME = Producer.class.getName();
    final private Logger logger = Logger.getLogger(PRODUCER_CLASS_NAME);

    public Producer(String host, String port) {
        
    }

}